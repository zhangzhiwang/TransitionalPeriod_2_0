package cursor.mall.service.interfaces;

import cursor.mall.model.HomeContent;

/**
 * 首页业务接口。
 * （由 Curosr 自动生成）
 */
public interface HomeService {

    /**
     * 组装首页聚合数据（当前为写死演示数据，后续可改为查库）。
     * （由 Curosr 自动生成）
     *
     * @return 首页内容
     */
    HomeContent buildHomeContent();
}
